<?php require dirname( __FILE__ ) . DIRECTORY_SEPARATOR . '_noncename.phtml'; ?>

<?php $description = esc_html( $this->description ); ?>

<?php if( $description <> '' ) : ?>
  <p>
    <?= $description; ?>
  </p>
<?php endif;?>

<?php wp_editor( $this->getValue( $this->id ), $this->id ); ?>

<script type="text/javascript">
  ( function( $ )
  {
    $( '#<?= $this->id ?>' )
      .css( 'display', 'none' );

    $( '#post-body-content' ).append( 
      $( '#wp-<?= $this->id ?>-wrap' )
        .css( 'margin-bottom', '20px' ) );

    $( document ).ready(
      function()
      {
        $( '#adv-settings label[for="<?= $this->id ?>-hide"]' )
          .css( 'display', 'none' );
      });
  })( jQuery );
</script>